extends layouts/main
include mixins/videoBlock

block content 
  .videos
    each item in videosdb 
     +videoBlock({
      title: item.title,
      views: item.views,
      videoFile: item.videoFile
     })
     