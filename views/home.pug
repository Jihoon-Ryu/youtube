extends layouts/main
include mixins/videoBlock

block content 
  .videos
    each item in videosdb 
     +videoBlock({
      id: item.id,
      title: item.title,
      views: item.views,
      videoFile: item.fileUrl })

     